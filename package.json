{
  "name": "tab-out",
  "version": "1.0.0",
  "private": true,
  "description": "Chrome extension that mirrors your Chrome tab groups on the new tab page",
  "license": "MIT",
  "scripts": {
    "test": "node test/app.test.js"
  }
}
